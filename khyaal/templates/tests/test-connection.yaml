apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "khyaal.fullname" . }}-test-connection"
  labels:
    {{- include "khyaal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "khyaal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
